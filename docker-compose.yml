version: '3.1'
services:
  springboot:
    image: dockerpracticereg/maven-web-app:VERSION
    restart: always
    ports:
    - 9090:8080
    networks:
    - mavenappbridge
networks:
  mavenappbridge:
